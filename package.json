{
  "name": "synthehol",
  "version": "0.1.0",
  "private": true,
  "description": "An interactive guide to analog synthesis — learn oscillators, filters, envelopes, and LFOs by playing with them.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:live": "bash scripts/dev-live.sh",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
